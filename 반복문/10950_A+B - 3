//2022년 6월 28일 화요일
//두 정수 A와 B를 입력받은 다음, A+B를 출력하는 프로그램을 작성하시오.
#pragma warning(disable: 4996)
#include <stdio.h>
int main() {
	int A, B, N;
	scanf("%d", &N);
	for (int i = 0; i < N; i++) {
		scanf("%d %d", &A, &B);
		printf("%d\n", A + B);
	}
}
