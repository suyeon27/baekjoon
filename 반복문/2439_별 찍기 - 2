//2022년 7월 1일 금요일
//오른쪽을 기준으로 정렬한 별 N개를 찍는 문제
#pragma warning(disable: 4996)
#include <stdio.h>
int main() {
	int n;
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			if (i + j <= n - 2)
				printf(" ");
			else
			printf("*");
		}
		printf("\n");
	}
}
